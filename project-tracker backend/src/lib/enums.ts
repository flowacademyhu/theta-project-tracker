export enum Roles {
    user = 'user',
    admin = 'admin'
}

export enum TableNames {
    users = 'users',
    clients = 'clients',
    projects = 'projects',
    milestones = 'milestones',
    projectUsers = 'projectUsers'
}
